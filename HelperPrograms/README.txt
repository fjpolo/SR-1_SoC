To use the AutoHotKey script for filling out the BSRAM details for you, just press Ctrl-l whilst having the IP Customization window active.

Make sure to replace the placeholder path with the path to the CPU Memory folder